<?xml version="1.0" encoding="utf-8"?>
    <androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.client.ClientMainActivity">

    <!-- Фрагмент для навигации -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment_client"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/bottom_navigation_height"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph_client" />

    <!-- Нижняя навигация -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_navigation_height"
        android:layout_gravity="bottom"
        android:background="@color/army_navy"
        app:activeIndicatorLabelPadding="@dimen/bottom_navigation_icon_label_spacing"
        app:itemActiveIndicatorStyle="@null"
        app:itemIconTint="@color/nav_item_color"
        app:itemPaddingBottom="@dimen/bottom_navigation_item_padding_bottom"
        app:itemPaddingTop="@dimen/bottom_navigation_item_padding_top"
        app:itemTextColor="@color/nav_item_color"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_client_bottom" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
